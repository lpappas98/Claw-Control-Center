# Multi-stage build for Claw Control Center UI
# Stage 1: Build stage
FROM node:22-alpine AS builder

WORKDIR /app

# Copy package files
COPY package*.json ./
COPY projects/claw-task-manager ./projects/claw-task-manager/

WORKDIR /app/projects/claw-task-manager

# Install dependencies
RUN npm ci

# Build the Vite project
RUN npm run build

# Stage 2: Runtime stage with Nginx
FROM nginx:alpine

# Install curl for health checks
RUN apk add --no-cache curl

# Copy nginx config
COPY docker/nginx.conf /etc/nginx/conf.d/default.conf

# Copy built artifacts from builder stage
COPY --from=builder /app/projects/claw-task-manager/dist /usr/share/nginx/html

# Create non-root user
RUN addgroup -g 1001 -S nginx && \
    adduser -S nginx -u 1001 && \
    chown -R nginx:nginx /usr/share/nginx/html /var/cache/nginx /var/run/nginx

USER nginx

# Expose port for the UI
EXPOSE 3000

# Health check
HEALTHCHECK --interval=30s --timeout=5s --start-period=10s --retries=3 \
    CMD curl -f http://localhost:3000/ || exit 1

# Labels for metadata
LABEL org.opencontainers.image.title="Claw Control Center UI"
LABEL org.opencontainers.image.description="React/Vite frontend for multi-agent task management system"
LABEL org.opencontainers.image.version="1.0.0"
